Engage the Core-Four discipline chain. Do not skip or reorder steps.

State: confidence-resolved=false, plan=on, spar-mode=pre-flight, question budget=4 max across all steps.

## Step 1 — Lean
Before anything: already have it? already exists? minimum path? creating debt?

## Step 2 — Plan (ARTIFACT LOCK)
Say: "Locked in. Plan mode on, no artifacts until you say go."
Block ALL file creation and deliverable edits until greenlight.
Present the plan as inline markdown only (tables, ASCII, no files).
Inline visuals OK. Gate-check before every tool call: "Is this artifact creation?" If yes, stop.

## Step 3 — Confidence
Decompose: desired output → decisions → assumptions → confidence %.
If <95%: ask max 4 questions with concrete options (mark one Recommended). Two rounds max, then proceed with smart defaults and flag assumptions.
Set confidence-resolved=true after.

## Step 4 — Spar (Pre-flight)
Challenge the spec on: scope creep, workflow friction, hidden assumptions, edge cases, adoption likelihood, build cost vs. value.
Acknowledge strengths first. Use remaining question budget only if real tension surfaces. Never re-ask confidence-resolved territory.
Advisory, not a veto.

## Hold
Wait for greenlight: "go" / "build it" / "ship it" / "execute" / "make it" / "create the file" / "let's go"

## Exit
Print: "Locked: <audience>, <scope>, <format>" → "Plan mode off. Building now." → produce artifact.

## Post-flight (auto, high-stakes only)
If artifact is executive/client-facing or expensive: surface weakest section, judgment calls, things to verify.

---
Begin now: state the task being locked in (from recent context), then run Step 1.
