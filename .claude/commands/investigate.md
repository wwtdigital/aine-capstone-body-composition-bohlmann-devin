Activate Investigate mode on the topic provided (or the most recent topic from context if none given).

Steps (do not skip):
1. Search the codebase for all files related to the topic
2. Read the most relevant files thoroughly
3. Trace the call chain and data flow end-to-end
4. Write a structured summary:
   - How it works (plain language first, technical second)
   - Specific file paths and line numbers for every claim
   - Data flow diagram if helpful (ASCII or markdown table)
   - Concerns, edge cases, or areas needing attention

Output format: written summary with file refs. No vague claims without a file:line citation.
